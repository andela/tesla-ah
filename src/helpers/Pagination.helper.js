/**
 * @author Eric rukundo
 * @class Pagination
 * @description this class performs the whole authentication
 */
class Pagination {
  /**
   * signup controller
   * @param {Integer} page  - Response
   * @returns {Object} The response object
   */
  static async pagianate(page) {
    return page;
  }
}

export default Pagination;
